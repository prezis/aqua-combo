# Agent Instructions

This is an aqua-combo plugin repo. When working on this codebase:
- Skills are in `skills/*/SKILL.md`
- GPU detection is in `scripts/gpu-detect.py`
- Tests are in `tests/`
- Run tests with `python3 -m pytest tests/ -v`
